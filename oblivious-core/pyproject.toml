[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "oblivious-core"
version = "0.1.0"
requires-python = ">=3.10"
description = "Constant-time oblivious top-K for the in-TEE matcher"

[tool.maturin]
features = ["python"]
module-name = "oblivious_core"
